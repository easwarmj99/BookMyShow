apiVersion: v1
kind: ConfigMap
metadata:
  name: bms-config
  namespace: bookmyshow
  labels:
    app: bookmyshow

data:
  APP_ENV: "production"
  LOG_LEVEL: "warn"
  PORT: "80"
  NGINX_WORKER_PROCESSES: "auto"
  NGINX_WORKER_CONNECTIONS: "1024"
